---
'@affex/auth-core': minor
'@affex/ui-kit': minor
'@affex/design-tokens': minor
---

Initial release. Promoted from OPTIONAL_LATER to ACTIVE_NOW (ADR 0004 + ADR 0005).

- `@affex/auth-core` v0.1.0: argon2 password helpers, `jose` JWT sign/verify, `getUserFromRequest` session helper.
- `@affex/ui-kit` v0.1.0: Button, Input, Label, Card primitives. shadcn pattern + Radix; cn() utility.
- `@affex/design-tokens` v0.1.0: Tailwind preset + CSS variables encoding `standards/DESIGN.md`.
